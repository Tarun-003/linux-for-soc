# 🔍 grep Command (Search in Logs)

grep is used to search for specific patterns or keywords inside files.

SOC analysts use grep to find suspicious activity in logs.

---

## 📌 Basic Syntax

```bash

grep "keyword" filename
